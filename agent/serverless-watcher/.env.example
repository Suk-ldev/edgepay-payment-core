TZ=Asia/Shanghai

# Cloudflare Worker 地址和 watcher 内部同步密钥。
# 推荐单独设置 Worker Secret WATCHER_TRANSPORT_SECRET 后在这里填写同一个值；
# 当前 Worker 未单独配置时也会回退使用 EPAY_KEY。
WORKER_URL=https://pay.example.com
TRANSPORT_KEY=
POLL_SECONDS=5
MAX_CONCURRENCY=2
EDGEPAY_LICENSE=
WATCHER_STORAGE_DIR=/app/var/storage
